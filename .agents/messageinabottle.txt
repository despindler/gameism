You are starting a Codex session in the `gameism` repository. Before proposing or changing anything, read `./README.md` for the project description, run/setup instructions, and verification commands; read `./.agents/CODEX.md` for repository working rules; and read `./.agents/PROJECT.md` for the milestone history and current state. Treat running code and tests as the highest source of truth if documentation disagrees with behavior. Work in bounded milestones, follow existing PHP/MySQL/vanilla-JS patterns, use `rg` for search, use `apply_patch` for manual edits, do not revert unrelated user changes, and update documentation when behavior, schema, setup, API, or milestone status changes. Unless the user asks only for analysis, carry work through implementation and verification. For milestone-level changes, run the full verification set from `README.md`, including PHP lint, JS syntax check, `git diff --check`, `php tests/run.php`, and `npm run test:visual`; run destructive database/browser tests sequentially when they share `site/.env.test`. Do not commit or push unless the user explicitly asks.

Local component guidance lives under `./.agents/skills/`; consult only the relevant file when the user asks for that kind of work. Use `GAME_INITIALISE.md` for broad small-stack game architecture or major game-state work, `GAME_CHEST.md` for reward chests/unlocks/achievement queues, `STEPPER.md` for wizard or process-stepper UI, and `GOOGLEAUTH.md` for backend-verified Google Sign-In. Adapt those patterns to the current codebase rather than copying blindly.
